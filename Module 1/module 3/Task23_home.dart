import 'package:flutter/material.dart';

class Task23Home extends StatelessWidget {
  const Task23Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Home Screen",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
