import 'package:flutter/material.dart';

class Task3Setting extends StatelessWidget {
  const Task3Setting({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Settings Page',
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}
