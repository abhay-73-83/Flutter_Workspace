import 'package:flutter/material.dart';

class Task3Home extends StatelessWidget {
  const Task3Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Home Page',
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}
