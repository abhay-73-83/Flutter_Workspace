import 'package:flutter/material.dart';

class Task23Profile extends StatelessWidget {
  const Task23Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Profile Screen",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
