import 'package:flutter/material.dart';

class Task23Settings extends StatelessWidget {
  const Task23Settings({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Settings Screen",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
