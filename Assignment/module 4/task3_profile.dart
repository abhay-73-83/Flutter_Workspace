import 'package:flutter/material.dart';

class Task3Profile extends StatelessWidget {
  const Task3Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Profile Page',
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}
